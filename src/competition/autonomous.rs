//! Implements competition autonomous control.

use anyhow::Result;
#[expect(unused_imports)]
use vexide::prelude::*;

use crate::prelude::*;

pub async fn autonomous_control(_robot: &mut CompetitionRobot) -> Result<()> {
    Ok(())
}
